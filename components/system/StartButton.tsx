import type { FC } from 'react';
import StyledStartButton from 'styles/components/StyledStartButton';

const StartButton: FC = () => <StyledStartButton>X</StyledStartButton>;

export default StartButton;
