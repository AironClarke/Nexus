import type { FC } from 'react';
import StyledTaskbarEntry from 'styles/components/StyledTaskbarEntry';

const TaskbarEntry: FC = () => <StyledTaskbarEntry>PROCESS</StyledTaskbarEntry>;

export default TaskbarEntry;
